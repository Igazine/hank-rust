[package]
name = "demo"
version = "0.1.0"
edition = "2021"

[dependencies]
hank = { path = "../../" }
regex = "1.10"
regex-lite = "0.1"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
